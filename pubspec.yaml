name: sudoku
description: Учебное приложение «Судоку»
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  # Локальное хранилище: чистый Dart, работает офлайн на всех платформах
  # (iOS/Android — файлы приложения, web — IndexedDB браузера).
  hive: ^2.2.3
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.25.0

flutter:
  uses-material-design: true
